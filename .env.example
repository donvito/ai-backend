# General Configuration
# Used to protect production endpoints (required in production)
DEFAULT_ACCESS_TOKEN=change-me

# CORS Configuration
# Comma-separated list of allowed origins. Supports `*` and wildcard segments like `https://*.example.com`.
# If unset, localhost (http/https, any port) is allowed by default.
# Do not include trailing slashes in origins.
CORS_ORIGINS=http://localhost:3000,https://localhost:3000
# Alternatively, you can use ALLOWED_ORIGINS (legacy alias)
# ALLOWED_ORIGINS=https://*.example.com,https://app.example.com

# API Base URL for documentation (optional, used when NODE_ENV=production)
# API_BASE_URL=https://api.yourdomain.com

# OpenAI Configuration (optional)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-nano
# OPENAI_BASE_URL=

# Anthropic Configuration (optional)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-haiku-20240307

# Ollama Configuration (optional)
# Set OLLAMA_ENABLED=true to mark Ollama as available (auto-enabled when OLLAMA_BASE_URL is set)
OLLAMA_ENABLED=true
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2:latest
OLLAMA_CHAT_MODEL=llama3.2:latest
OLLAMA_TIMEOUT=30000

# OpenRouter Configuration (optional)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
# OPENROUTER_BASE_URL=

# Node environment
# NODE_ENV=production